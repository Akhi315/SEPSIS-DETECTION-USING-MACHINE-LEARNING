<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>Sepsis Prediction Dashboard</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
  :root{
    --bg:#0f172a;           /* slate-900 */
    --card:#111827;         /* gray-900 */
    --muted:#9ca3af;        /* gray-400 */
    --text:#e5e7eb;         /* gray-200 */
    --brand:#4f46e5;        /* indigo-600 */
    --brand-2:#22c55e;      /* green-500 */
    --warn:#ef4444;         /* red-500 */
    --chip-ok:#10b981;      /* emerald-500 */
    --chip-bad:#f43f5e;     /* rose-500 */
    --chip-neutral:#64748b; /* slate-500 */
    --shadow: 0 10px 25px rgba(0,0,0,0.35);
    --radius: 14px;
  }
  *{ box-sizing: border-box }
  body{
    margin:0; font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
    background: radial-gradient(1200px 600px at 10% -10%, rgba(79,70,229,.25), transparent),
                radial-gradient(1200px 600px at 110% 10%, rgba(34,197,94,.18), transparent),
                var(--bg);
    color: var(--text);
  }
  .container{ max-width:1100px; margin: 40px auto; padding: 0 20px; }
  .header{
    display:flex; align-items:center; justify-content:space-between; gap:16px; margin-bottom:24px;
  }
  .title{
    display:flex; align-items:center; gap:12px;
  }
  .title h1{ margin:0; font-size:28px; letter-spacing:.2px }
  .subtitle{ color:var(--muted); margin-top:6px; font-size:14px }
  .grid{
    display:grid; grid-template-columns: 1.1fr .9fr; gap:24px;
  }
  .card{
    background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    padding:20px;
  }
  .card h3{ margin:0 0 14px 0; font-size:18px; letter-spacing:.2px }
  .live{
    display:grid; grid-template-columns: repeat(2, 1fr); gap:12px; margin-bottom:12px;
  }
  .metric{
    background: rgba(255,255,255,0.03);
    border:1px solid rgba(255,255,255,0.06);
    border-radius: 12px; padding:12px;
  }
  .metric .label{ color:var(--muted); font-size:13px }
  .metric .value{ font-size:18px; margin-top:6px }
  .btn{
    width:100%; padding:11px 14px; border-radius:10px; border:none; cursor:pointer;
    color:white; background: linear-gradient(90deg, var(--brand), #7c3aed);
    transition: transform .04s ease, filter .2s ease;
  }
  .btn:hover{ filter: brightness(1.05) }
  .btn:active{ transform: translateY(1px) }
  .btn-secondary{
    background: linear-gradient(90deg, #334155, #0f172a);
  }

  form .row{ display:grid; grid-template-columns: repeat(2, 1fr); gap:12px }
  form label{ font-size:13px; color: var(--muted) }
  input, select{
    width:100%; margin-top:6px; padding:10px 12px; border-radius:10px; border:1px solid rgba(255,255,255,0.14);
    background:#0b1220; color:var(--text); outline:none;
  }
  input::placeholder{ color:#6b7280 }
  .models{
    display:grid; grid-template-columns: repeat(2, 1fr); gap:8px; margin:8px 0 2px 0;
    color:var(--muted);
  }
  .chip{
    display:inline-flex; align-items:center; gap:8px; padding:8px 12px; border-radius:999px; font-size:13px;
    border:1px solid rgba(255,255,255,0.08);
    background: rgba(255,255,255,0.03);
  }
  .chip.ok{ background: rgba(16,185,129,.12); border-color: rgba(16,185,129,.25) }
  .chip.bad{ background: rgba(244,63,94,.12); border-color: rgba(244,63,94,.25) }
  .verdict{
    display:flex; align-items:center; justify-content:space-between; gap:12px; margin-top:12px;
    background: linear-gradient(180deg, rgba(34,197,94,.12), rgba(34,197,94,.06));
    border:1px solid rgba(34,197,94,.25); padding:14px; border-radius: 12px;
  }
  .verdict.bad{
    background: linear-gradient(180deg, rgba(244,63,94,.12), rgba(244,63,94,.06));
    border-color: rgba(244,63,94,.25);
  }
  .muted{ color: var(--muted) }
  .spacer{ height:6px }
  @media (max-width: 900px){
    .grid{ grid-template-columns: 1fr; }
  }
</style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="title">
        <div style="width:36px;height:36px;border-radius:10px;background:linear-gradient(135deg,var(--brand),var(--brand-2));box-shadow:0 6px 14px rgba(79,70,229,.35)"></div>
        <div>
          <h1>Sepsis Prediction</h1>
          <div class="subtitle">Enter vitals or click Fill to copy current live measurements into the form.</div>
        </div>
      </div>
    </div>

    <div class="grid">
      <!-- Left: Live card -->
      <div class="card">
        <h3>📡 Live Sensor Data</h3>
        <div class="live">
          <div class="metric">
            <div class="label">Heart Rate</div>
            <div class="value">❤️ {{ sensor_data.Heart_Rate }} bpm</div>
          </div>
          <div class="metric">
            <div class="label">SpO₂</div>
            <div class="value">🩸 {{ sensor_data.SpO2 }} %</div>
          </div>
          <div class="metric">
            <div class="label">Temperature</div>
            <div class="value">🌡 {{ sensor_data.Body_Temperature }} °C</div>
          </div>
          <div class="metric">
            <div class="label">Respiratory Rate</div>
            <div class="value">🌬 {{ sensor_data.Respiratory_Rate }} /min</div>
          </div>
        </div>
        <button type="button" class="btn btn-secondary" id="btn-fill">Fill form with live values</button>
      </div>

      <!-- Right: Form + Results -->
      <div class="card">
        <h3>🧮 Prediction</h3>
        <form method="POST" id="pred-form">
          <div class="row">
            <div>
              <label>Mode</label>
              <select name="mode" id="mode">
                <option value="manual" selected>Manual</option>
                <option value="live">Live</option>
              </select>
            </div>
            <div>
              <label>Age</label>
              <input type="number" name="Age" id="Age" min="0" placeholder="e.g., 45" required>
            </div>
          </div>
          <div class="row">
            <div>
              <label>Sex</label>
              <select name="Sex" id="Sex">
                <option value="Male">Male</option>
                <option value="Female">Female</option>
              </select>
            </div>
            <div>
              <label>Heart Rate</label>
              <input type="number" name="Heart_Rate" id="Heart_Rate" step="any" placeholder="e.g., 78">
            </div>
          </div>
          <div class="row">
            <div>
              <label>SpO₂</label>
              <input type="number" name="SpO2" id="SpO2" step="any" placeholder="e.g., 97">
            </div>
            <div>
              <label>Body Temperature (°C)</label>
              <input type="number" name="Body_Temperature" id="Body_Temperature" step="any" placeholder="e.g., 36.8">
            </div>
          </div>
          <div class="row">
            <div>
              <label>Respiratory Rate</label>
              <input type="number" name="Respiratory_Rate" id="Respiratory_Rate" step="any" placeholder="e.g., 16">
            </div>
            <div>
              <label>Select Models</label>
              <div class="models">
                <label><input type="checkbox" name="models" value="Logistic Regression"> Logistic Regression</label>
                <label><input type="checkbox" name="models" value="Random Forest"> Random Forest</label>
                <label><input type="checkbox" name="models" value="Decision Tree"> Decision Tree</label>
                <label><input type="checkbox" name="models" value="SVM"> SVM</label>
                <label><input type="checkbox" name="models" value="KNN"> KNN</label>
              </div>
            </div>
          </div>

          <div class="spacer"></div>
          <button type="submit" class="btn">Predict</button>

          {% if prediction_results %}
            <div style="margin-top:16px">
              <div class="muted" style="margin-bottom:8px">Model predictions</div>
              <div style="display:flex; flex-wrap:wrap; gap:8px">
                {% for model, result in prediction_results.items() %}
                  {% set ok = "No Sepsis" in result %}
                  <div class="chip {{ 'ok' if ok else 'bad' }}">
                    <span style="opacity:.9">{{ model }}</span>
                    <strong>{{ result }}</strong>
                  </div>
                {% endfor %}
              </div>
            </div>
            <div class="spacer"></div>
            {% set final_bad = final_prediction and ('Sepsis Detected' in final_prediction) %}
            <div class="verdict {{ 'bad' if final_bad else '' }}">
              <div style="font-size:16px">
                <strong>Final Prediction:</strong> {{ final_prediction }}
              </div>
              <div class="muted">Calibrated decision</div>
            </div>
          {% endif %}
        </form>
      </div>
    </div>
  </div>

<script>
  // Cached live values from Flask
  const liveValues = {
    Heart_Rate: Number("{{ sensor_data.Heart_Rate }}"),
    SpO2: Number("{{ sensor_data.SpO2 }}"),
    Body_Temperature: Number("{{ sensor_data.Body_Temperature }}"),
    Respiratory_Rate: Number("{{ sensor_data.Respiratory_Rate }}"),
  };
  // Fill form on click (no auto-overwrite)
  document.getElementById('btn-fill').addEventListener('click', () => {
    const set = (id, v) => document.getElementById(id).value = (Number.isFinite(v) ? v : "");
    set('Heart_Rate', liveValues.Heart_Rate);
    set('SpO2', liveValues.SpO2);
    set('Body_Temperature', liveValues.Body_Temperature);
    set('Respiratory_Rate', liveValues.Respiratory_Rate);
  });
</script>
</body>
</html>
